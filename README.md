# FastAPI Calculator

## 🚀 What it does

A simple API to perform basic arithmetic operations (`+`, `-`, `*`, `/`) using FastAPI.

## 🧪 How to Run

### Locally
```bash
uvicorn app.main:app --reload

